# MONGO DB environment information file
# mongoDB URL to connect to the mongoDB instance
# accessible from process.env.MONGO (refer to .env-cmdrc)
MONGO_HOST 	mongodb service host
MONGO_PORT 	mongodb service port (27017)
MONGO_USER 	mongodb user
MONGO_PW	mongodb password corresponding to USER
MONGO_DB	mongodb database name (minxpressnode)

# the access url looks as follow (password based)
#URL=`mongodb://${USER}:${PW}@${HOST}:${PORT}/${DB}?retryWrites=true&w=majority`

# TLS options 1/0 or true false
MONGO_TLS 		tls control. 1 - use tls, 0 - don't use tls [insecure]
MONGO_TLS_ALLOWINVALID 	tls allow invalid. 1 - allow, 0 - disallow [self-signed or unverified certificates]
# specify file location
MONGO_TLS_CAFILE 	tls cafile used to validate mongo service host (/home/cjason/cj-authority.crt)
